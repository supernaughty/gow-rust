// Wave 0: scaffold tests — placeholder to verify crate compiles.
// Wave 2 will replace these with full round-trip integration tests (R019).

#[test]
fn wave0_placeholder() {
    // This test exists to verify the crate scaffolding compiles cleanly.
    // Real integration tests (compress/decompress round-trips) are added in Wave 2.
    assert!(true);
}
