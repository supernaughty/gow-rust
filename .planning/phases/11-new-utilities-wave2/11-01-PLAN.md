---
phase: 11-new-utilities-wave2
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - extras/bin/[.bat
  - crates/gow-whoami/Cargo.toml
  - crates/gow-whoami/build.rs
  - crates/gow-whoami/src/main.rs
  - crates/gow-whoami/src/lib.rs
  - crates/gow-whoami/tests/integration.rs
  - crates/gow-uname/Cargo.toml
  - crates/gow-uname/build.rs
  - crates/gow-uname/src/main.rs
  - crates/gow-uname/src/lib.rs
  - crates/gow-uname/tests/integration.rs
  - crates/gow-paste/Cargo.toml
  - crates/gow-paste/build.rs
  - crates/gow-paste/src/main.rs
  - crates/gow-paste/src/lib.rs
  - crates/gow-paste/tests/integration.rs
  - crates/gow-join/Cargo.toml
  - crates/gow-join/build.rs
  - crates/gow-join/src/main.rs
  - crates/gow-join/src/lib.rs
  - crates/gow-join/tests/integration.rs
  - crates/gow-split/Cargo.toml
  - crates/gow-split/build.rs
  - crates/gow-split/src/main.rs
  - crates/gow-split/src/lib.rs
  - crates/gow-split/tests/integration.rs
  - crates/gow-printf/Cargo.toml
  - crates/gow-printf/build.rs
  - crates/gow-printf/src/main.rs
  - crates/gow-printf/src/lib.rs
  - crates/gow-printf/tests/integration.rs
  - crates/gow-expr/Cargo.toml
  - crates/gow-expr/build.rs
  - crates/gow-expr/src/main.rs
  - crates/gow-expr/src/lib.rs
  - crates/gow-expr/tests/integration.rs
  - crates/gow-test/Cargo.toml
  - crates/gow-test/build.rs
  - crates/gow-test/src/main.rs
  - crates/gow-test/src/lib.rs
  - crates/gow-test/tests/integration.rs
  - crates/gow-fmt/Cargo.toml
  - crates/gow-fmt/build.rs
  - crates/gow-fmt/src/main.rs
  - crates/gow-fmt/src/lib.rs
  - crates/gow-fmt/tests/integration.rs
  - crates/gow-unlink/Cargo.toml
  - crates/gow-unlink/build.rs
  - crates/gow-unlink/src/main.rs
  - crates/gow-unlink/src/lib.rs
  - crates/gow-unlink/tests/integration.rs
autonomous: true
requirements:
  - U2-01
  - U2-02
  - U2-03
  - U2-04
  - U2-05
  - U2-06
  - U2-07
  - U2-08
  - U2-09
  - U2-10

must_haves:
  truths:
    - "cargo build --workspace exits 0 after scaffold (all 10 new crates compile as stubs)"
    - "Each new stub binary prints '<name>: not implemented' and exits 1 when invoked"
    - "Root Cargo.toml workspace.members lists all 10 new Phase 11 crates"
    - "Root Cargo.toml windows-sys entry has Win32_System_WindowsProgramming, Win32_System_SystemInformation, and Wdk_System_SystemServices features added"
    - "extras/bin/[.bat exists with content: @echo off & \"%~dp0test.exe\" --_bracket_ %*"
    - "All 10 test files contain Wave 0 placeholder tests that compile (cargo test --workspace --no-run exits 0)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace members + expanded windows-sys features"
      contains: "gow-whoami"
    - path: "extras/bin/[.bat"
      provides: "Bracket alias shim dispatching to test.exe with sentinel flag"
      contains: "--_bracket_"
    - path: "crates/gow-whoami/Cargo.toml"
      provides: "gow-whoami crate config (binary: whoami, lib: uu_whoami, dep: windows-sys)"
      contains: "windows-sys"
    - path: "crates/gow-expr/Cargo.toml"
      provides: "gow-expr crate config (binary: expr, lib: uu_expr, dep: regex)"
      contains: "regex"
    - path: "crates/gow-paste/Cargo.toml"
      provides: "gow-paste crate config (binary: paste, lib: uu_paste, dep: bstr)"
      contains: "bstr"
  key_links:
    - from: "Cargo.toml workspace.members"
      to: "10 new Phase 11 crate paths"
      via: "workspace member entries"
      pattern: "\"crates/gow-whoami\""
    - from: "Cargo.toml windows-sys features"
      to: "Win32_System_WindowsProgramming, Win32_System_SystemInformation, Wdk_System_SystemServices"
      via: "features list"
      pattern: "Win32_System_WindowsProgramming"
    - from: "extras/bin/[.bat"
      to: "test.exe --_bracket_"
      via: "batch shim"
      pattern: "--_bracket_"
---

<objective>
Scaffold ten new workspace crates for Phase 11 (gow-whoami, gow-uname, gow-paste, gow-join, gow-split, gow-printf, gow-expr, gow-test, gow-fmt, gow-unlink), wire them into the root Cargo.toml, add three new windows-sys feature flags (Win32_System_WindowsProgramming, Win32_System_SystemInformation, Wdk_System_SystemServices), and create the extras/bin/[.bat shim for the test/[ bracket alias.

Purpose: Wave 2 plans depend on all 10 crates existing with correct deps declared. The windows-sys feature additions enable GetUserNameW (whoami) and RtlGetVersion/GetNativeSystemInfo (uname) without separate cargo registry downloads. The [.bat shim is source-controlled alongside existing extras/bin shims so the MSI heat harvester picks it up automatically.

Output: ~52 new files, 1 modified Cargo.toml, 1 new extras/bin/[.bat, workspace build green.
</objective>

<execution_context>
@C:/Users/super/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/super/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-new-utilities-wave2/11-RESEARCH.md

<interfaces>
Key contracts the executor needs. Extracted from codebase.

From crates/gow-df/Cargo.toml (exact template for single-binary crates, copy and adapt):
```toml
[package]
name = "gow-df"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "GNU df — Windows port."

[[bin]]
name = "df"
path = "src/main.rs"

[lib]
name = "uu_df"
path = "src/lib.rs"

[dependencies]
gow-core = { path = "../gow-core" }
clap = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
windows-sys = { workspace = true }   # only for crates that need Win32 APIs

[build-dependencies]
embed-manifest = "1.5"

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
tempfile = { workspace = true }
```

From crates/gow-df/build.rs (verbatim copy for ALL 10 build.rs files):
```rust
fn main() {
    println!("cargo:rerun-if-changed=build.rs");
    println!("cargo:rerun-if-changed=Cargo.toml");

    if std::env::var_os("CARGO_CFG_WINDOWS").is_some() {
        embed_manifest::embed_manifest(
            embed_manifest::new_manifest("Gow.Rust")
                .active_code_page(embed_manifest::manifest::ActiveCodePage::Utf8)
                .long_path_aware(embed_manifest::manifest::Setting::Enabled),
        )
        .expect("unable to embed manifest");
    }
}
```

From crates/gow-df/src/main.rs (3-line entrypoint pattern):
```rust
fn main() {
    std::process::exit(uu_df::uumain(std::env::args_os()));
}
```

Stub lib.rs pattern (replace <name> with the binary name):
```rust
use std::ffi::OsString;

pub fn uumain<I: IntoIterator<Item = OsString>>(_args: I) -> i32 {
    gow_core::init();
    eprintln!("<name>: not implemented");
    1
}
```

Wave 0 integration test placeholder (identical for all 10 crates):
```rust
//! Wave 0 placeholder — replaced by real integration tests in Wave 2+.

#[test]
fn scaffold_compiles() {
    assert!(true);
}
```

Current windows-sys entry in Cargo.toml (lines 85-89) — MUST be replaced with expanded version:
```toml
windows-sys = { version = "0.61", features = [
    "Win32_System_Console",
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
] }
```

[.bat shim content (entire file, single line):
```bat
@echo off & "%~dp0test.exe" --_bracket_ %*
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 10 workspace members and expand windows-sys features in root Cargo.toml; create extras/bin/[.bat</name>
  <files>
    Cargo.toml
    extras/bin/[.bat
  </files>
  <read_first>
    - Cargo.toml: Read full file. Current last workspace member is "crates/gow-hashsum" (line 62). The windows-sys entry is around lines 85-89. New Phase 11 member block goes after the Phase 10 block (after "crates/gow-hashsum") before the closing `]`. The windows-sys features list must be REPLACED (not appended) to add 3 new features.
    - extras/bin/ directory: Confirm existing .bat files to match style (no newline at end, @echo off pattern).
  </read_first>
  <action>
Edit Cargo.toml with two changes.

**Change 1 — Workspace members.** After the `"crates/gow-hashsum",` line and BEFORE the closing `]` of the members array, insert:

```toml
    # Phase 11 — new utilities wave 2 (U2-01 through U2-10)
    "crates/gow-whoami",
    "crates/gow-uname",
    "crates/gow-paste",
    "crates/gow-join",
    "crates/gow-split",
    "crates/gow-printf",
    "crates/gow-expr",
    "crates/gow-test",
    "crates/gow-fmt",
    "crates/gow-unlink",
```

**Change 2 — windows-sys features.** Replace the existing windows-sys entry:
```toml
windows-sys = { version = "0.61", features = [
    "Win32_System_Console",
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
] }
```
with:
```toml
windows-sys = { version = "0.61", features = [
    "Win32_System_Console",
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
    "Win32_System_WindowsProgramming",    # Phase 11: GetUserNameW (whoami) + GetComputerNameW (uname)
    "Win32_System_SystemInformation",     # Phase 11: OSVERSIONINFOW/GetNativeSystemInfo (uname)
    "Wdk_System_SystemServices",          # Phase 11: RtlGetVersion (uname)
] }
```

Do NOT modify any other entries. Do NOT re-add walkdir/bstr/regex (already workspace deps).

**Create extras/bin/[.bat** (new file, single line — no trailing newline):
```
@echo off & "%~dp0test.exe" --_bracket_ %*
```

The `--_bracket_` sentinel flag tells test.exe it was invoked as `[` and should require a trailing `]` argument.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --manifest-path Cargo.toml --format-version 1</automated>
  </verify>
  <acceptance_criteria>
    - grep "gow-whoami" Cargo.toml returns at least 1 match
    - grep "gow-uname" Cargo.toml returns at least 1 match
    - grep "gow-paste" Cargo.toml returns at least 1 match
    - grep "gow-join" Cargo.toml returns at least 1 match
    - grep "gow-split" Cargo.toml returns at least 1 match
    - grep "gow-printf" Cargo.toml returns at least 1 match
    - grep "gow-expr" Cargo.toml returns at least 1 match
    - grep "gow-test" Cargo.toml returns at least 1 match
    - grep "gow-fmt" Cargo.toml returns at least 1 match
    - grep "gow-unlink" Cargo.toml returns at least 1 match
    - grep "Win32_System_WindowsProgramming" Cargo.toml returns at least 1 match
    - grep "Win32_System_SystemInformation" Cargo.toml returns at least 1 match
    - grep "Wdk_System_SystemServices" Cargo.toml returns at least 1 match
    - cat extras/bin/[.bat contains "--_bracket_"
    - cargo metadata --no-deps --manifest-path Cargo.toml exits 0 (TOML valid — will fail if Task 2 crate dirs don't exist yet; run Task 2 first then re-verify)
  </acceptance_criteria>
  <done>Root Cargo.toml has 10 new Phase 11 workspace members; windows-sys features include the 3 new Phase 11 flags; extras/bin/[.bat shim exists with --_bracket_ sentinel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold all 10 crates with stub uumain + Wave 0 test files</name>
  <files>
    crates/gow-whoami/Cargo.toml
    crates/gow-whoami/build.rs
    crates/gow-whoami/src/main.rs
    crates/gow-whoami/src/lib.rs
    crates/gow-whoami/tests/integration.rs
    crates/gow-uname/Cargo.toml
    crates/gow-uname/build.rs
    crates/gow-uname/src/main.rs
    crates/gow-uname/src/lib.rs
    crates/gow-uname/tests/integration.rs
    crates/gow-paste/Cargo.toml
    crates/gow-paste/build.rs
    crates/gow-paste/src/main.rs
    crates/gow-paste/src/lib.rs
    crates/gow-paste/tests/integration.rs
    crates/gow-join/Cargo.toml
    crates/gow-join/build.rs
    crates/gow-join/src/main.rs
    crates/gow-join/src/lib.rs
    crates/gow-join/tests/integration.rs
    crates/gow-split/Cargo.toml
    crates/gow-split/build.rs
    crates/gow-split/src/main.rs
    crates/gow-split/src/lib.rs
    crates/gow-split/tests/integration.rs
    crates/gow-printf/Cargo.toml
    crates/gow-printf/build.rs
    crates/gow-printf/src/main.rs
    crates/gow-printf/src/lib.rs
    crates/gow-printf/tests/integration.rs
    crates/gow-expr/Cargo.toml
    crates/gow-expr/build.rs
    crates/gow-expr/src/main.rs
    crates/gow-expr/src/lib.rs
    crates/gow-expr/tests/integration.rs
    crates/gow-test/Cargo.toml
    crates/gow-test/build.rs
    crates/gow-test/src/main.rs
    crates/gow-test/src/lib.rs
    crates/gow-test/tests/integration.rs
    crates/gow-fmt/Cargo.toml
    crates/gow-fmt/build.rs
    crates/gow-fmt/src/main.rs
    crates/gow-fmt/src/lib.rs
    crates/gow-fmt/tests/integration.rs
    crates/gow-unlink/Cargo.toml
    crates/gow-unlink/build.rs
    crates/gow-unlink/src/main.rs
    crates/gow-unlink/src/lib.rs
    crates/gow-unlink/tests/integration.rs
  </files>
  <read_first>
    - crates/gow-df/Cargo.toml: exact template for single-binary crate structure
    - crates/gow-df/build.rs: verbatim copy for all 10 build.rs files
    - crates/gow-df/src/main.rs: 3-line entrypoint template
  </read_first>
  <action>
Create all 50 new files. All crates are single-binary (no multi-binary crates in Phase 11).

**Per-crate table** — for each crate, create 5 files: Cargo.toml, build.rs, src/main.rs, src/lib.rs, tests/integration.rs.

| Crate dir | Binary name | Lib name | Description | Extra deps |
|-----------|-------------|----------|-------------|------------|
| crates/gow-whoami | whoami | uu_whoami | "GNU whoami — Windows port." | windows-sys = { workspace = true } |
| crates/gow-uname | uname | uu_uname | "GNU uname — Windows port." | windows-sys = { workspace = true } |
| crates/gow-paste | paste | uu_paste | "GNU paste — Windows port." | bstr = { workspace = true } |
| crates/gow-join | join | uu_join | "GNU join — Windows port." | bstr = { workspace = true } |
| crates/gow-split | split | uu_split | "GNU split — Windows port." | bstr = { workspace = true } |
| crates/gow-printf | printf | uu_printf | "GNU printf — Windows port." | (none extra) |
| crates/gow-expr | expr | uu_expr | "GNU expr — Windows port." | regex = { workspace = true } |
| crates/gow-test | test | uu_test | "GNU test — Windows port." | (none extra) |
| crates/gow-fmt | fmt | uu_fmt | "GNU fmt — Windows port." | bstr = { workspace = true } |
| crates/gow-unlink | unlink | uu_unlink | "GNU unlink — Windows port." | (none extra) |

**Each Cargo.toml** follows this template (substitute values from the table above):
```toml
[package]
name = "gow-<crate-suffix>"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "<description from table>"

[[bin]]
name = "<binary name>"
path = "src/main.rs"

[lib]
name = "<lib name>"
path = "src/lib.rs"

[dependencies]
gow-core = { path = "../gow-core" }
clap = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
<extra deps from table, if any>

[build-dependencies]
embed-manifest = "1.5"

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
tempfile = { workspace = true }
```

IMPORTANT: Do NOT add `features = [...]` to windows-sys in any crate Cargo.toml. Write only `windows-sys = { workspace = true }`. The workspace already declares the full feature set; crates inherit it automatically.

**Each build.rs** — verbatim copy of crates/gow-df/build.rs (read it first):
```rust
fn main() {
    println!("cargo:rerun-if-changed=build.rs");
    println!("cargo:rerun-if-changed=Cargo.toml");

    if std::env::var_os("CARGO_CFG_WINDOWS").is_some() {
        embed_manifest::embed_manifest(
            embed_manifest::new_manifest("Gow.Rust")
                .active_code_page(embed_manifest::manifest::ActiveCodePage::Utf8)
                .long_path_aware(embed_manifest::manifest::Setting::Enabled),
        )
        .expect("unable to embed manifest");
    }
}
```

**Each src/main.rs** — 3-line entrypoint (substitute lib name):
```rust
fn main() {
    std::process::exit(uu_<libname>::uumain(std::env::args_os()));
}
```
Examples: `uu_whoami`, `uu_uname`, `uu_paste`, `uu_join`, `uu_split`, `uu_printf`, `uu_expr`, `uu_test`, `uu_fmt`, `uu_unlink`.

**Each src/lib.rs** — stub (substitute binary name in eprintln):
```rust
use std::ffi::OsString;

pub fn uumain<I: IntoIterator<Item = OsString>>(_args: I) -> i32 {
    gow_core::init();
    eprintln!("<binary-name>: not implemented");
    1
}
```

**Each tests/integration.rs** — Wave 0 placeholder:
```rust
//! Wave 0 placeholder — replaced by real integration tests in Wave 2+.

#[test]
fn scaffold_compiles() {
    assert!(true);
}
```

CRITICAL constraints:
- Do NOT add tokio anywhere
- Do NOT add features = [...] to windows-sys in crate Cargo.toml — workspace inheritance handles it
- Each tests/integration.rs MUST have at least the scaffold_compiles test
- No multi-binary crates in Phase 11 — every crate has exactly one [[bin]] entry
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - cargo build --workspace exits 0
    - cargo test --workspace --no-run exits 0 (all Wave 0 test files compile)
    - grep "not implemented" crates/gow-whoami/src/lib.rs returns 1 match (stub present)
    - grep "not implemented" crates/gow-test/src/lib.rs returns 1 match
    - grep "windows-sys" crates/gow-whoami/Cargo.toml returns 1 match
    - grep "windows-sys" crates/gow-uname/Cargo.toml returns 1 match
    - grep "regex" crates/gow-expr/Cargo.toml returns 1 match
    - grep "bstr" crates/gow-paste/Cargo.toml returns 1 match
    - grep "bstr" crates/gow-join/Cargo.toml returns 1 match
    - grep "bstr" crates/gow-split/Cargo.toml returns 1 match
    - grep "bstr" crates/gow-fmt/Cargo.toml returns 1 match
    - grep "tokio" crates/gow-whoami/Cargo.toml returns NO match (and same for all 10 new crates)
    - grep "features" crates/gow-whoami/Cargo.toml returns NO match for windows-sys features line (workspace inheritance only)
  </acceptance_criteria>
  <done>All 50 new files created; workspace build exits 0; cargo test --workspace --no-run exits 0; each stub binary prints "not implemented" with exit 1; no multi-binary crates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS to build scripts | build.rs executes during cargo build; embed-manifest writes a manifest into each .exe — trusted, MSVC linker path |
| extras/bin/[.bat to test.exe | Batch shim invokes test.exe with --_bracket_ sentinel; sentinel is internal-only, not exposed to callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | windows-sys supply chain (new features added) | accept | Verified features exist in windows-sys 0.61.2 source; cargo audits supply chain; no C code pulled in by feature additions |
| T-11-01-02 | Information Disclosure | Stub uumain prints crate name on stderr | accept | Standard error message pattern throughout project; no sensitive info in stub |
| T-11-01-03 | Tampering | [.bat shim calling test.exe via %~dp0 | accept | %~dp0 resolves to the directory of [.bat, same as test.exe install dir; no PATH injection risk since path is explicit |
</threat_model>

<verification>
Run in order after both tasks complete:

```bash
# 1. Workspace TOML parses
cargo metadata --no-deps --manifest-path Cargo.toml --format-version 1 > /dev/null

# 2. Workspace compiles
cargo build --workspace

# 3. All test files compile
cargo test --workspace --no-run

# 4. Spot-check stub behavior
cargo run -p gow-whoami 2>&1 | head -1
# Expected: "whoami: not implemented"

cargo run -p gow-test 2>&1 | head -1
# Expected: "test: not implemented"

# 5. Confirm windows-sys features in workspace
grep "Win32_System_WindowsProgramming" Cargo.toml
grep "Wdk_System_SystemServices" Cargo.toml

# 6. Confirm [.bat shim
cat extras/bin/[.bat
# Expected: @echo off & "%~dp0test.exe" --_bracket_ %*
```
</verification>

<success_criteria>
- cargo build --workspace exits 0 after scaffold
- All 10 new crates listed in cargo metadata --no-deps output
- Root Cargo.toml windows-sys features include Win32_System_WindowsProgramming, Win32_System_SystemInformation, Wdk_System_SystemServices
- extras/bin/[.bat exists and contains --_bracket_ sentinel
- Each stub binary prints "<name>: not implemented" with exit 1
- All Wave 0 test files compile (cargo test --workspace --no-run exits 0)
- No regressions: existing Phase 10 crates still build
</success_criteria>

<output>
After completion, create `.planning/phases/11-new-utilities-wave2/11-01-SUMMARY.md`
</output>
