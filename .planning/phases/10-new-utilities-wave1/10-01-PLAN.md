---
phase: 10-new-utilities-wave1
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/gow-seq/Cargo.toml
  - crates/gow-seq/build.rs
  - crates/gow-seq/src/main.rs
  - crates/gow-seq/src/lib.rs
  - crates/gow-seq/tests/integration.rs
  - crates/gow-sleep/Cargo.toml
  - crates/gow-sleep/build.rs
  - crates/gow-sleep/src/main.rs
  - crates/gow-sleep/src/lib.rs
  - crates/gow-sleep/tests/integration.rs
  - crates/gow-tac/Cargo.toml
  - crates/gow-tac/build.rs
  - crates/gow-tac/src/main.rs
  - crates/gow-tac/src/lib.rs
  - crates/gow-tac/tests/integration.rs
  - crates/gow-nl/Cargo.toml
  - crates/gow-nl/build.rs
  - crates/gow-nl/src/main.rs
  - crates/gow-nl/src/lib.rs
  - crates/gow-nl/tests/integration.rs
  - crates/gow-od/Cargo.toml
  - crates/gow-od/build.rs
  - crates/gow-od/src/main.rs
  - crates/gow-od/src/lib.rs
  - crates/gow-od/tests/integration.rs
  - crates/gow-fold/Cargo.toml
  - crates/gow-fold/build.rs
  - crates/gow-fold/src/main.rs
  - crates/gow-fold/src/lib.rs
  - crates/gow-fold/tests/integration.rs
  - crates/gow-expand-unexpand/Cargo.toml
  - crates/gow-expand-unexpand/build.rs
  - crates/gow-expand-unexpand/src/expand.rs
  - crates/gow-expand-unexpand/src/unexpand.rs
  - crates/gow-expand-unexpand/src/lib.rs
  - crates/gow-expand-unexpand/tests/integration.rs
  - crates/gow-du/Cargo.toml
  - crates/gow-du/build.rs
  - crates/gow-du/src/main.rs
  - crates/gow-du/src/lib.rs
  - crates/gow-du/tests/integration.rs
  - crates/gow-df/Cargo.toml
  - crates/gow-df/build.rs
  - crates/gow-df/src/main.rs
  - crates/gow-df/src/lib.rs
  - crates/gow-df/tests/integration.rs
  - crates/gow-hashsum/Cargo.toml
  - crates/gow-hashsum/build.rs
  - crates/gow-hashsum/src/md5sum.rs
  - crates/gow-hashsum/src/sha1sum.rs
  - crates/gow-hashsum/src/sha256sum.rs
  - crates/gow-hashsum/src/lib.rs
  - crates/gow-hashsum/tests/integration.rs
autonomous: true
requirements:
  - U-01
  - U-02
  - U-03
  - U-04
  - U-05
  - U-06
  - U-07
  - U-08
  - U-09
  - U-10

must_haves:
  truths:
    - "cargo build --workspace exits 0 after scaffold (all 10 new crates compile as stubs)"
    - "Each new binary prints '<name>: not implemented' and exits 1 when invoked"
    - "gow-expand-unexpand produces TWO binaries: expand.exe and unexpand.exe"
    - "gow-hashsum produces THREE binaries: md5sum.exe, sha1sum.exe, sha256sum.exe"
    - "Root Cargo.toml workspace.members lists all 10 new crates"
    - "Root Cargo.toml workspace.dependencies has md-5, sha1, sha2, digest, hex entries"
    - "All 10 test files contain Wave 0 placeholder tests that compile (cargo test --workspace --no-run exits 0)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace members + new hash workspace deps"
      contains: "gow-hashsum"
    - path: "crates/gow-seq/Cargo.toml"
      provides: "gow-seq crate config (binary name: seq, lib: uu_seq)"
      contains: "name = \"seq\""
    - path: "crates/gow-expand-unexpand/Cargo.toml"
      provides: "Multi-binary crate (expand + unexpand)"
      contains: "name = \"unexpand\""
    - path: "crates/gow-hashsum/Cargo.toml"
      provides: "Multi-binary crate (md5sum + sha1sum + sha256sum)"
      contains: "name = \"sha256sum\""
    - path: "crates/gow-df/Cargo.toml"
      provides: "gow-df crate config with windows-sys"
      contains: "windows-sys"
    - path: "crates/gow-du/Cargo.toml"
      provides: "gow-du crate config with walkdir"
      contains: "walkdir"
    - path: "crates/gow-hashsum/src/lib.rs"
      provides: "Stub uumain proving md-5/sha1/sha2/digest/hex link"
      contains: "uumain"
  key_links:
    - from: "Cargo.toml workspace.members"
      to: "10 new crate paths"
      via: "workspace member entries"
      pattern: "\"crates/gow-seq\""
    - from: "crates/gow-hashsum/Cargo.toml"
      to: "md-5, sha1, sha2, digest, hex workspace deps"
      via: "workspace = true"
      pattern: "(md-5|sha1|sha2|digest|hex)"
    - from: "crates/gow-df/Cargo.toml"
      to: "windows-sys workspace dep with Win32_Storage_FileSystem feature (already in workspace)"
      via: "windows-sys = { workspace = true }"
      pattern: "windows-sys"
---

<objective>
Scaffold ten new workspace crates (gow-seq, gow-sleep, gow-tac, gow-nl, gow-od, gow-fold, gow-expand-unexpand, gow-du, gow-df, gow-hashsum), wire them into the root Cargo.toml, add new hash workspace deps (md-5, sha1, sha2, digest, hex), and prove the multi-binary crate pattern compiles for gow-expand-unexpand (expand + unexpand) and gow-hashsum (md5sum + sha1sum + sha256sum).

Purpose: Wave 2+ plans depend on these crates existing with correct deps declared. The hash crate stub MUST link md-5/sha1/sha2/digest/hex so a future plan failure cannot be caused by missing crate registration. Each multi-binary crate must produce all its binaries (expand + unexpand; md5sum + sha1sum + sha256sum) per the gow-gzip pattern (gzip + gunzip + zcat).

Output: ~52 new files, 1 modified Cargo.toml, workspace build green.
</objective>

<execution_context>
@C:/Users/super/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/super/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-new-utilities-wave1/10-RESEARCH.md

<interfaces>
Key contracts the executor needs. Extracted from codebase.

From crates/gow-gzip/src/main.rs (3-line pattern, copy verbatim, swap library name):

```rust
fn main() {
    std::process::exit(uu_gzip::uumain(std::env::args_os()));
}
```

From crates/gow-gzip/build.rs (verbatim copy for ALL 10 build.rs files):

```rust
fn main() {
    println!("cargo:rerun-if-changed=build.rs");
    println!("cargo:rerun-if-changed=Cargo.toml");

    if std::env::var_os("CARGO_CFG_WINDOWS").is_some() {
        embed_manifest::embed_manifest(
            embed_manifest::new_manifest("Gow.Rust")
                .active_code_page(embed_manifest::manifest::ActiveCodePage::Utf8)
                .long_path_aware(embed_manifest::manifest::Setting::Enabled),
        )
        .expect("unable to embed manifest");
    }
}
```

From crates/gow-gzip/Cargo.toml (multi-binary pattern, adapt for expand-unexpand and hashsum):

```toml
[package]
name = "gow-gzip"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "GNU gzip/gunzip/zcat — Windows port."

[[bin]]
name = "gzip"
path = "src/main.rs"

[[bin]]
name = "gunzip"
path = "src/gunzip.rs"

[[bin]]
name = "zcat"
path = "src/zcat.rs"

[lib]
name = "uu_gzip"
path = "src/lib.rs"

[dependencies]
gow-core = { path = "../gow-core" }
clap = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
flate2 = { workspace = true }

[build-dependencies]
embed-manifest = "1.5"

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
tempfile = { workspace = true }
```

Stub lib.rs pattern (for all 10 crates, replace placeholder with utility name):

```rust
use std::ffi::OsString;

pub fn uumain<I: IntoIterator<Item = OsString>>(_args: I) -> i32 {
    gow_core::init();
    eprintln!("seq: not implemented");
    1
}
```

Workspace deps to add (from RESEARCH.md, Workspace Cargo.toml Additions):

```toml
md-5 = "0.11"
sha1 = "0.11"
sha2 = "0.11"
digest = "0.11"
hex = "0.4"
```

NOTE on RustCrypto crate naming:
- crates.io: md-5 (hyphen)
- Rust import: use md5::Md5; (underscore, no hyphen)
- The `[dependencies]` line in gow-hashsum/Cargo.toml is `md-5 = { workspace = true }` but Rust code writes `use md5::Md5;`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 10 workspace members and 5 new workspace deps to root Cargo.toml</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml: read full file. Last members entry is "crates/gow-curl"; last workspace.dependencies block is the reqwest entry around line 117. New entries must be appended AFTER these blocks, before the [profile.release] section.
    - .planning/phases/10-new-utilities-wave1/10-RESEARCH.md: section "Workspace Cargo.toml Additions" specifies exact text.
  </read_first>
  <action>
Edit Cargo.toml with two additions.

Addition 1: Workspace members. After the `"crates/gow-curl",` line (last Phase 6 entry, line 51) and BEFORE the closing `]`, add:

```toml
    # Phase 10 — new utilities wave 1 (U-01 through U-10)
    "crates/gow-seq",
    "crates/gow-sleep",
    "crates/gow-tac",
    "crates/gow-nl",
    "crates/gow-od",
    "crates/gow-fold",
    "crates/gow-expand-unexpand",
    "crates/gow-du",
    "crates/gow-df",
    "crates/gow-hashsum",
```

Addition 2: Workspace dependencies. At the END of the [workspace.dependencies] block (after the reqwest block ending around line 117) and BEFORE the [profile.release] section, add:

```toml

# Phase 10 additions (U-10 hash utilities)
md-5 = "0.11"                        # MD5 hash function — gow-hashsum only
sha1 = "0.11"                        # SHA-1 hash function — gow-hashsum only
sha2 = "0.11"                        # SHA-256 — gow-hashsum only
digest = "0.11"                      # Hash trait — gow-hashsum only
hex = "0.4"                          # Digest hex encoding — gow-hashsum only
```

Do NOT modify any existing entries. Do NOT add features to windows-sys (Win32_Storage_FileSystem is already enabled). Do NOT add walkdir/bstr again (already workspace deps).
  </action>
  <verify>
    <automated>cargo metadata --no-deps --manifest-path Cargo.toml --format-version 1</automated>
  </verify>
  <acceptance_criteria>
    - grep "gow-seq" Cargo.toml returns at least 1 match
    - grep "gow-sleep" Cargo.toml returns at least 1 match
    - grep "gow-tac" Cargo.toml returns at least 1 match
    - grep "gow-nl" Cargo.toml returns at least 1 match
    - grep "gow-od" Cargo.toml returns at least 1 match
    - grep "gow-fold" Cargo.toml returns at least 1 match
    - grep "gow-expand-unexpand" Cargo.toml returns at least 1 match
    - grep "gow-du" Cargo.toml returns at least 1 match
    - grep "gow-df" Cargo.toml returns at least 1 match
    - grep "gow-hashsum" Cargo.toml returns at least 1 match
    - grep -E "^md-5 = \"0\\.11\"" Cargo.toml finds a match
    - grep -E "^sha1 = \"0\\.11\"" Cargo.toml finds a match
    - grep -E "^sha2 = \"0\\.11\"" Cargo.toml finds a match
    - grep -E "^digest = \"0\\.11\"" Cargo.toml finds a match
    - grep -E "^hex = \"0\\.4\"" Cargo.toml finds a match
    - cargo metadata --no-deps --manifest-path Cargo.toml exits 0 (TOML parses, all members exist as directories — fails if Task 2 has not run yet, in which case run Task 2 first then re-verify)
  </acceptance_criteria>
  <done>Root Cargo.toml workspace.members has all 10 new crate paths; workspace.dependencies has md-5=0.11, sha1=0.11, sha2=0.11, digest=0.11, hex=0.4 entries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold all 10 crates with stub uumain + Wave 0 test files</name>
  <files>
    crates/gow-seq/Cargo.toml
    crates/gow-seq/build.rs
    crates/gow-seq/src/main.rs
    crates/gow-seq/src/lib.rs
    crates/gow-seq/tests/integration.rs
    crates/gow-sleep/Cargo.toml
    crates/gow-sleep/build.rs
    crates/gow-sleep/src/main.rs
    crates/gow-sleep/src/lib.rs
    crates/gow-sleep/tests/integration.rs
    crates/gow-tac/Cargo.toml
    crates/gow-tac/build.rs
    crates/gow-tac/src/main.rs
    crates/gow-tac/src/lib.rs
    crates/gow-tac/tests/integration.rs
    crates/gow-nl/Cargo.toml
    crates/gow-nl/build.rs
    crates/gow-nl/src/main.rs
    crates/gow-nl/src/lib.rs
    crates/gow-nl/tests/integration.rs
    crates/gow-od/Cargo.toml
    crates/gow-od/build.rs
    crates/gow-od/src/main.rs
    crates/gow-od/src/lib.rs
    crates/gow-od/tests/integration.rs
    crates/gow-fold/Cargo.toml
    crates/gow-fold/build.rs
    crates/gow-fold/src/main.rs
    crates/gow-fold/src/lib.rs
    crates/gow-fold/tests/integration.rs
    crates/gow-expand-unexpand/Cargo.toml
    crates/gow-expand-unexpand/build.rs
    crates/gow-expand-unexpand/src/expand.rs
    crates/gow-expand-unexpand/src/unexpand.rs
    crates/gow-expand-unexpand/src/lib.rs
    crates/gow-expand-unexpand/tests/integration.rs
    crates/gow-du/Cargo.toml
    crates/gow-du/build.rs
    crates/gow-du/src/main.rs
    crates/gow-du/src/lib.rs
    crates/gow-du/tests/integration.rs
    crates/gow-df/Cargo.toml
    crates/gow-df/build.rs
    crates/gow-df/src/main.rs
    crates/gow-df/src/lib.rs
    crates/gow-df/tests/integration.rs
    crates/gow-hashsum/Cargo.toml
    crates/gow-hashsum/build.rs
    crates/gow-hashsum/src/md5sum.rs
    crates/gow-hashsum/src/sha1sum.rs
    crates/gow-hashsum/src/sha256sum.rs
    crates/gow-hashsum/src/lib.rs
    crates/gow-hashsum/tests/integration.rs
  </files>
  <read_first>
    - crates/gow-gzip/Cargo.toml: exact template for multi-binary crate (the [[bin]], [lib], [dependencies], [build-dependencies], [dev-dependencies] blocks)
    - crates/gow-gzip/build.rs: verbatim copy for all 10 build.rs files
    - crates/gow-gzip/src/main.rs: 3-line main.rs template
    - crates/gow-gzip/src/gunzip.rs: proves alternate-binary entrypoint is one fn main() calling the same uumain
  </read_first>
  <action>
Create all 52 new files. Each crate Cargo.toml uses the skeleton shown in the interfaces block, with deps adapted per the per-crate table below. Each build.rs is a VERBATIM copy of crates/gow-gzip/build.rs. Each main.rs is the 3-line `fn main() { std::process::exit(uu_LIBNAME::uumain(std::env::args_os())); }` pattern. Each lib.rs is the stub pattern from the interfaces block (replace the eprintln message with the binary name).

Each tests/integration.rs is a Wave 0 placeholder so cargo test --workspace --no-run stays green:

```rust
//! Wave 0 placeholder — replaced by real integration tests in Wave 2+.

#[test]
fn scaffold_compiles() {
    assert!(true);
}
```

Per-crate specifics (single-binary crates):

| Crate dir | Binary name | Lib name | Description | Extra deps |
|-----------|-------------|----------|-------------|------------|
| crates/gow-seq | seq | uu_seq | "GNU seq — Windows port." | (none extra) |
| crates/gow-sleep | sleep | uu_sleep | "GNU sleep — Windows port." | (none extra) |
| crates/gow-tac | tac | uu_tac | "GNU tac — Windows port." | bstr = { workspace = true } |
| crates/gow-nl | nl | uu_nl | "GNU nl — Windows port." | bstr = { workspace = true } |
| crates/gow-od | od | uu_od | "GNU od — Windows port." | (none extra) |
| crates/gow-fold | fold | uu_fold | "GNU fold — Windows port." | bstr = { workspace = true } |
| crates/gow-du | du | uu_du | "GNU du — Windows port." | walkdir = { workspace = true } |
| crates/gow-df | df | uu_df | "GNU df — Windows port." | windows-sys = { workspace = true } |

Multi-binary crate gow-expand-unexpand: TWO [[bin]] entries. Cargo.toml:

```toml
[package]
name = "gow-expand-unexpand"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "GNU expand/unexpand — Windows port."

[[bin]]
name = "expand"
path = "src/expand.rs"

[[bin]]
name = "unexpand"
path = "src/unexpand.rs"

[lib]
name = "uu_expand_unexpand"
path = "src/lib.rs"

[dependencies]
gow-core = { path = "../gow-core" }
clap = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
bstr = { workspace = true }

[build-dependencies]
embed-manifest = "1.5"

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
tempfile = { workspace = true }
```

- crates/gow-expand-unexpand/src/expand.rs: `fn main() { std::process::exit(uu_expand_unexpand::uumain(std::env::args_os())); }`
- crates/gow-expand-unexpand/src/unexpand.rs: IDENTICAL byte-for-byte to expand.rs (argv[0] dispatch happens inside uumain in Wave 2)
- crates/gow-expand-unexpand/src/lib.rs: stub uumain, eprintln "expand: not implemented", exit 1

Multi-binary crate gow-hashsum: THREE [[bin]] entries. Cargo.toml:

```toml
[package]
name = "gow-hashsum"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "GNU md5sum/sha1sum/sha256sum — Windows port."

[[bin]]
name = "md5sum"
path = "src/md5sum.rs"

[[bin]]
name = "sha1sum"
path = "src/sha1sum.rs"

[[bin]]
name = "sha256sum"
path = "src/sha256sum.rs"

[lib]
name = "uu_hashsum"
path = "src/lib.rs"

[dependencies]
gow-core = { path = "../gow-core" }
clap = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
md-5 = { workspace = true }
sha1 = { workspace = true }
sha2 = { workspace = true }
digest = { workspace = true }
hex = { workspace = true }

[build-dependencies]
embed-manifest = "1.5"

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
tempfile = { workspace = true }
```

- crates/gow-hashsum/src/md5sum.rs, sha1sum.rs, sha256sum.rs: each is the 3-line `fn main() { std::process::exit(uu_hashsum::uumain(std::env::args_os())); }`. All three files identical content.
- crates/gow-hashsum/src/lib.rs: stub PLUS unused-import lines that force linkage of all five hash crates (proves the deps actually compile and link on this MSVC toolchain — same canary technique used in 06-01 for liblzma):

```rust
use std::ffi::OsString;

// Linkage canary — these imports force md-5/sha1/sha2/digest/hex to compile
// and link during the scaffold wave. Wave 4 replaces with real usage.
#[allow(unused_imports)]
use digest::Digest;
#[allow(unused_imports)]
use md5::Md5;
#[allow(unused_imports)]
use sha1::Sha1;
#[allow(unused_imports)]
use sha2::Sha256;

pub fn uumain<I: IntoIterator<Item = OsString>>(_args: I) -> i32 {
    gow_core::init();
    // Touch hex::encode at runtime so the symbol is referenced (linker keeps it).
    let _ = hex::encode([0u8; 0]);
    eprintln!("hashsum: not implemented");
    1
}
```

CRITICAL constraints (do not violate):
- Do NOT add tokio anywhere
- Do NOT add windows-sys feature lists in any crate Cargo.toml — the workspace already has Win32_Storage_FileSystem enabled and inheritance via { workspace = true } carries the features
- Each tests/integration.rs MUST contain at minimum the scaffold_compiles test so the test binary has a target

After all 52 files exist, run `cargo build --workspace` to confirm. If gow-hashsum compiles, the canary is satisfied (md-5/sha1/sha2/digest/hex all linked).
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - cargo build --workspace exits 0 (all 10 new crates compile as stubs)
    - cargo build -p gow-hashsum exits 0 specifically (md-5/sha1/sha2/digest/hex MSVC link canary)
    - All 10 lib.rs files exist (one per crate)
    - grep "not implemented" crates/gow-seq/src/lib.rs finds a match (and same for sleep/tac/nl/od/fold/du/df/expand-unexpand)
    - grep "tokio" returns NO match in any new crate Cargo.toml or src file
    - cargo test --workspace --no-run exits 0 (all Wave 0 test files compile)
    - ls target/debug/expand.exe and target/debug/unexpand.exe both exist after cargo build (proves multi-binary dispatch)
    - ls target/debug/md5sum.exe and target/debug/sha1sum.exe and target/debug/sha256sum.exe all exist after cargo build
    - cargo run -p gow-hashsum --bin md5sum -- --any-flag prints "hashsum: not implemented" and exits 1
  </acceptance_criteria>
  <done>All 52 new files created; workspace build exits 0; gow-hashsum hash crate link canary passes; expand+unexpand and md5sum+sha1sum+sha256sum binaries all produced; each stub binary prints its "not implemented" message on invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS to build scripts | build.rs executes during cargo build; embed-manifest writes a manifest into each .exe — trusted, MSVC linker path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | RustCrypto crates (md-5, sha1, sha2, digest, hex) supply chain | accept | Verified crates from crates.io 2026-04-29; cargo audits supply chain; pure-Rust no-C build |
| T-10-01-02 | Information disclosure | Stub uumain prints crate name on stderr | accept | Standard error message format used throughout project; no sensitive info |
</threat_model>

<verification>
Run in order after task completion:

```bash
# 1. Workspace TOML parses
cargo metadata --no-deps --manifest-path Cargo.toml --format-version 1 > /dev/null

# 2. Workspace compiles
cargo build --workspace

# 3. Hash crate link canary specifically
cargo build -p gow-hashsum

# 4. All test files compile
cargo test --workspace --no-run

# 5. Multi-binary outputs exist
ls target/debug/expand.exe target/debug/unexpand.exe
ls target/debug/md5sum.exe target/debug/sha1sum.exe target/debug/sha256sum.exe

# 6. Stub behavior
cargo run -p gow-seq -- --help 2>&1 | head -3
cargo run -p gow-hashsum --bin md5sum -- --help 2>&1 | head -3
```

All commands must exit 0 (cargo build/test --no-run) or print the "<name>: not implemented" stub message.
</verification>

<success_criteria>
- cargo build --workspace exits 0 after scaffold
- cargo build -p gow-hashsum exits 0 (md-5/sha1/sha2/digest/hex MSVC link canary passes)
- All 10 new crates listed in cargo metadata --no-deps output
- Root Cargo.toml workspace.deps has md-5=0.11, sha1=0.11, sha2=0.11, digest=0.11, hex=0.4
- gow-expand-unexpand produces expand.exe AND unexpand.exe
- gow-hashsum produces md5sum.exe AND sha1sum.exe AND sha256sum.exe
- Each stub binary prints "<name>: not implemented" with exit code 1
- All Wave 0 test files compile (cargo test --workspace --no-run exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/10-new-utilities-wave1/10-01-SUMMARY.md`
</output>
