---
phase: "04-s04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/gow-grep/Cargo.toml
  - crates/gow-grep/build.rs
  - crates/gow-grep/src/main.rs
  - crates/gow-grep/src/lib.rs
  - crates/gow-sed/Cargo.toml
  - crates/gow-sed/build.rs
  - crates/gow-sed/src/main.rs
  - crates/gow-sed/src/lib.rs
  - crates/gow-sort/Cargo.toml
  - crates/gow-sort/build.rs
  - crates/gow-sort/src/main.rs
  - crates/gow-sort/src/lib.rs
  - crates/gow-uniq/Cargo.toml
  - crates/gow-uniq/build.rs
  - crates/gow-uniq/src/main.rs
  - crates/gow-uniq/src/lib.rs
  - crates/gow-tr/Cargo.toml
  - crates/gow-tr/build.rs
  - crates/gow-tr/src/main.rs
  - crates/gow-tr/src/lib.rs
  - crates/gow-cut/Cargo.toml
  - crates/gow-cut/build.rs
  - crates/gow-cut/src/main.rs
  - crates/gow-cut/src/lib.rs
  - crates/gow-diff/Cargo.toml
  - crates/gow-diff/build.rs
  - crates/gow-diff/src/main.rs
  - crates/gow-diff/src/lib.rs
  - crates/gow-patch/Cargo.toml
  - crates/gow-patch/build.rs
  - crates/gow-patch/src/main.rs
  - crates/gow-patch/src/lib.rs
  - crates/gow-awk/Cargo.toml
  - crates/gow-awk/build.rs
  - crates/gow-awk/src/main.rs
  - crates/gow-awk/src/lib.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 9 crates are listed as workspace members in root Cargo.toml"
    - "cargo build --workspace exits 0"
    - "cargo check --workspace exits 0"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace member list including all 9 S04 crates"
      contains: "gow-grep"
    - path: "crates/gow-grep/src/lib.rs"
      provides: "Stub with uumain entry point"
    - path: "crates/gow-diff/src/lib.rs"
      provides: "Stub with uumain entry point"
  key_links:
    - from: "Cargo.toml"
      to: "crates/gow-*/Cargo.toml"
      via: "workspace members array"
      pattern: "members = \\["
---

<objective>
Scaffold all 9 text processing crates (grep, sed, sort, uniq, tr, cut, diff, patch, awk) and register them in the workspace Cargo.toml.

Purpose: Foundation for the S04 text processing phase. Every crate needs a valid Cargo.toml with workspace inheritance, build.rs for Windows manifest embedding, 3-line main.rs, and a compilable stub lib.rs before implementation can proceed.
Output: 9 new crate directories, each building cleanly as workspace members.
</objective>

<execution_context>
@C:/Users/super/workspace/gow-rust/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/super/workspace/gow-rust/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/super/workspace/gow-rust/.planning/PROJECT.md
@C:/Users/super/workspace/gow-rust/.planning/ROADMAP.md
@C:/Users/super/workspace/gow-rust/.planning/STATE.md
@C:/Users/super/workspace/gow-rust/.planning/phases/04-s04/04-RESEARCH.md

<interfaces>
<!-- Pattern from crates/gow-sed/src/main.rs -->
```rust
fn main() {
    std::process::exit(uu_sed::uumain(std::env::args_os()));
}
```

<!-- Pattern from crates/gow-sed/src/lib.rs stub -->
```rust
use std::ffi::OsString;
pub fn uumain<I: IntoIterator<Item = OsString>>(_args: I) -> i32 {
    gow_core::init();
    eprintln!("gow-sed: not implemented");
    1
}
```

<!-- Cargo.toml workspace inheritance pattern from crates/gow-sed/Cargo.toml -->
```toml
[package]
name = "gow-sed"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "GNU sed — Windows port."

[[bin]]
name = "sed"
path = "src/main.rs"

[lib]
name = "uu_sed"
path = "src/lib.rs"

[dependencies]
gow-core = { path = "../gow-core" }
clap = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }

[build-dependencies]
embed-manifest = "1.5"

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 9 S04 crates in workspace and create scaffold files</name>
  <read_first>
    - Cargo.toml (root workspace — confirm existing members before adding)
    - crates/gow-sed/Cargo.toml (workspace inheritance template)
    - crates/gow-sed/build.rs (build.rs template to copy verbatim)
    - crates/gow-sed/src/main.rs (3-line main.rs template)
  </read_first>
  <files>
    Cargo.toml,
    crates/gow-grep/Cargo.toml, crates/gow-grep/build.rs, crates/gow-grep/src/main.rs, crates/gow-grep/src/lib.rs,
    crates/gow-sed/Cargo.toml, crates/gow-sed/build.rs, crates/gow-sed/src/main.rs, crates/gow-sed/src/lib.rs,
    crates/gow-sort/Cargo.toml, crates/gow-sort/build.rs, crates/gow-sort/src/main.rs, crates/gow-sort/src/lib.rs,
    crates/gow-uniq/Cargo.toml, crates/gow-uniq/build.rs, crates/gow-uniq/src/main.rs, crates/gow-uniq/src/lib.rs,
    crates/gow-tr/Cargo.toml, crates/gow-tr/build.rs, crates/gow-tr/src/main.rs, crates/gow-tr/src/lib.rs,
    crates/gow-cut/Cargo.toml, crates/gow-cut/build.rs, crates/gow-cut/src/main.rs, crates/gow-cut/src/lib.rs,
    crates/gow-diff/Cargo.toml, crates/gow-diff/build.rs, crates/gow-diff/src/main.rs, crates/gow-diff/src/lib.rs,
    crates/gow-patch/Cargo.toml, crates/gow-patch/build.rs, crates/gow-patch/src/main.rs, crates/gow-patch/src/lib.rs,
    crates/gow-awk/Cargo.toml, crates/gow-awk/build.rs, crates/gow-awk/src/main.rs, crates/gow-awk/src/lib.rs
  </files>
  <action>
    1. Add to [workspace] members in Cargo.toml under a "# Phase 4 — text processing (S04)" comment:
       "crates/gow-grep", "crates/gow-sed", "crates/gow-sort", "crates/gow-uniq",
       "crates/gow-tr", "crates/gow-cut", "crates/gow-diff", "crates/gow-patch", "crates/gow-awk"

    2. For each of the 9 crates, create these files using gow-sed as template:

       Cargo.toml — substitute {util} (e.g. "grep") and binary name. Keep minimal deps:
       gow-core, clap, anyhow, thiserror (workspace). embed-manifest 1.5 in build-deps.

       build.rs — copy verbatim from crates/gow-sed/build.rs (Windows application manifest embedding).

       src/main.rs (3 lines):
         fn main() { std::process::exit(uu_{util}::uumain(std::env::args_os())); }

       src/lib.rs (stub calling gow_core::init()):
         use std::ffi::OsString;
         pub fn uumain<I: IntoIterator<Item = OsString>>(_args: I) -> i32 {
             gow_core::init();
             eprintln!("gow-{util}: not implemented");
             1
         }

    3. Run `cargo check --workspace` and confirm exit 0.
  </action>
  <verify>
    <automated>cargo check --workspace</automated>
  </verify>
  <done>
    cargo check --workspace exits 0.
    Root Cargo.toml members array contains "crates/gow-grep", "crates/gow-diff", "crates/gow-awk".
    All 9 crate directories exist under crates/ with Cargo.toml, build.rs, src/main.rs, src/lib.rs.
    Each lib.rs contains `gow_core::init()`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace Cargo.toml | Dependency version authority for all 9 crates |
| build.rs embed-manifest | Injects Windows application manifest at compile time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Cargo.lock dependency pins | accept | Scaffold only; no external crates added beyond existing workspace deps. Cargo.lock is committed and reviewed. |
| T-04-01-02 | Tampering | embed-manifest build.rs | accept | embed-manifest pins Windows UAC/DPI manifest — low attack surface at scaffold stage. |
</threat_model>

<verification>
cargo check --workspace exits 0
cargo build --workspace exits 0
All 9 crate dirs exist with required files
</verification>

<success_criteria>
Workspace builds cleanly. All 9 S04 crates are registered workspace members with valid scaffolding. Each crate calls gow_core::init() in its stub uumain.
</success_criteria>

<output>
After completion, create `.planning/phases/04-s04/04-01-SUMMARY.md`
</output>
