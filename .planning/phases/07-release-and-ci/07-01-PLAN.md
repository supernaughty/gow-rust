---
phase: 07-release-and-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
autonomous: false
requirements:
  - REL-01
  - REL-02
  - REL-03

must_haves:
  truths:
    - "A v0.1.0 GitHub Release exists and is publicly visible"
    - "The release has two downloadable MSI assets: gow-rust-v0.1.0-installer-x64.msi and gow-rust-v0.1.0-installer-x86.msi"
    - "CONTRIBUTING.md exists and contains ARM64 build prerequisites and the build.bat command"
    - "gow-probe.exe is absent from both MSI files (already guaranteed by build.bat staging step)"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "ARM64 build documentation (REL-02)"
      contains: "aarch64-pc-windows-msvc"
  key_links:
    - from: "git tag v0.1.0"
      to: "GitHub Release"
      via: "gh release create"
      pattern: "v0.1.0"
    - from: "target/gow-rust-v0.1.0-installer-x64.msi"
      to: "GitHub Release assets"
      via: "gh release create --attach"
      pattern: "installer-x64.msi"
---

<objective>
Create the v0.1.0 GitHub Release with both MSI installers attached, and document the ARM64 build process in CONTRIBUTING.md.

Purpose: This is the first public release of gow-rust. Users can download native Windows MSI installers from GitHub Releases. ARM64 users have documented build instructions so they can produce their own MSI without guidance from the maintainer.

Output:
- Git tag `v0.1.0` pushed to origin
- GitHub Release `v0.1.0` with two MSI assets: `gow-rust-v0.1.0-installer-x64.msi` and `gow-rust-v0.1.0-installer-x86.msi`
- `CONTRIBUTING.md` with ARM64 build prerequisites and command
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRIBUTING.md with ARM64 build instructions</name>
  <files>CONTRIBUTING.md</files>
  <action>
Create CONTRIBUTING.md at the repo root. Content must include the following sections (per REL-02):

## Building from Source

### x64 (default)
```
build.bat installer x64
```
Output: `target\gow-rust-v<version>-installer-x64.msi`

### x86
```
build.bat installer x86
```
Output: `target\gow-rust-v<version>-installer-x86.msi`

### ARM64

ARM64 MSI is built via cross-compilation on any x64 Windows machine. No ARM64 hardware is required.

**Prerequisites:**
1. Visual Studio 2022 with "Desktop development with C++" workload
2. VS optional component: "MSVC v143 - VS 2022 C++ ARM64 build tools"
3. VS optional component: "Windows 11 SDK (10.0.22000 or later)"
4. `rustup target add aarch64-pc-windows-msvc`
5. WiX Toolset v3.14.1 — run `setup.bat` to install

**Build command:**
```
build.bat installer arm64
```

Output: `target\gow-rust-v<version>-installer-arm64.msi`

Also include a brief "Running Tests" section: `build.bat test` (runs `cargo test --workspace`).

Do NOT include a "Contributing Guidelines" section that would imply process — this project uses GSD workflow.
  </action>
  <verify>
    <automated>grep -l "aarch64-pc-windows-msvc" CONTRIBUTING.md</automated>
  </verify>
  <done>CONTRIBUTING.md exists at repo root, contains ARM64 prerequisites list with `rustup target add aarch64-pc-windows-msvc` and `build.bat installer arm64` command, and contains the x64/x86 build commands.</done>
</task>

<task type="auto">
  <name>Task 2: Tag v0.1.0 and push to origin</name>
  <files></files>
  <action>
Run the following commands in sequence:

```bash
git tag v0.1.0
git push origin v0.1.0
```

The workspace version in Cargo.toml is already `0.1.0` (verified). The tag must exist on origin before the GitHub Release is created in the next step.

Note: Do NOT create an annotated tag (`git tag -a`) — a lightweight tag is sufficient for the release trigger pattern `v*` in release.yml.
  </action>
  <verify>
    <automated>git tag --list v0.1.0</automated>
  </verify>
  <done>`git tag --list v0.1.0` returns `v0.1.0`. The tag is pushed to origin (verify with `git ls-remote origin refs/tags/v0.1.0`).</done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub Release v0.1.0 and upload MSI assets</name>
  <files></files>
  <action>
Run the `gh release create` command to create the v0.1.0 GitHub Release and attach both MSI files:

```bash
gh release create v0.1.0 \
  target/gow-rust-v0.1.0-installer-x64.msi \
  target/gow-rust-v0.1.0-installer-x86.msi \
  --title "gow-rust v0.1.0" \
  --notes "Initial release of gow-rust — GNU utilities for Windows, built with Rust.

## Downloads
- **gow-rust-v0.1.0-installer-x64.msi** — Windows x64 (most users)
- **gow-rust-v0.1.0-installer-x86.msi** — Windows x86 (32-bit)

ARM64: See [CONTRIBUTING.md](CONTRIBUTING.md) for build instructions.

## Included Utilities
awk, basename, cat, chmod, cp, curl, cut, diff, dirname, dos2unix, echo, env, false, find, grep, gunzip, gzip, head, less, ln, ls, mkdir, mv, patch, pwd, rm, rmdir, sed, sort, tail, tar, tee, touch, tr, true, uniq, unix2dos, wc, which, xargs, xz, yes, zcat"
```

Both MSI files already exist at `target/gow-rust-v0.1.0-installer-x64.msi` and `target/gow-rust-v0.1.0-installer-x86.msi` (built in the prior quick task 260429-q01).

REL-03 (gow-probe exclusion) is already satisfied: build.bat step 2 filters `gow-probe.exe` via PowerShell `Where-Object { $_.Name -ne 'gow-probe.exe' }` — the staged MSIs do not contain gow-probe.exe.
  </action>
  <verify>
    <automated>gh release view v0.1.0 --json assets --jq '[.assets[].name] | sort'</automated>
  </verify>
  <done>`gh release view v0.1.0` shows a published release with two assets: `gow-rust-v0.1.0-installer-x64.msi` and `gow-rust-v0.1.0-installer-x86.msi`. Release URL is visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Git tag v0.1.0 pushed to origin; GitHub Release v0.1.0 created with both MSI assets attached; CONTRIBUTING.md written with ARM64 prerequisites.</what-built>
  <how-to-verify>
1. Visit the GitHub Releases page for this repository
2. Confirm a release named "gow-rust v0.1.0" is visible
3. Confirm two MSI assets are attached: `gow-rust-v0.1.0-installer-x64.msi` and `gow-rust-v0.1.0-installer-x86.msi`
4. Optionally download one MSI and verify installation
5. Check `CONTRIBUTING.md` exists in the repo root with ARM64 build steps
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 07-02, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → GitHub origin | Tag push; only maintainers with push access can create tags |
| GitHub Actions GITHUB_TOKEN → release API | Release creation; scoped to contents: write in release.yml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | git tag v0.1.0 | accept | Only maintainers with push access can push tags; no signing required at v0.1.0 |
| T-07-01-02 | Information Disclosure | gh release create | accept | MSIs are public release artifacts; no secrets in release notes or asset names |
| T-07-01-03 | Elevation of Privilege | GitHub release creation via gh CLI | accept | Uses GITHUB_TOKEN scoped to authenticated user; no over-privileged service account |
</threat_model>

<verification>
- `git tag --list v0.1.0` returns `v0.1.0`
- `git ls-remote origin refs/tags/v0.1.0` returns the tag SHA
- `gh release view v0.1.0 --json assets --jq '[.assets[].name]'` returns both MSI filenames
- `grep -l "aarch64-pc-windows-msvc" CONTRIBUTING.md` returns `CONTRIBUTING.md`
</verification>

<success_criteria>
- GitHub Release v0.1.0 is publicly visible with two MSI assets (REL-01)
- CONTRIBUTING.md documents ARM64 build prerequisites and `build.bat installer arm64` command (REL-02)
- gow-probe.exe is absent from both MSIs — guaranteed by the existing build.bat staging filter (REL-03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-and-ci/07-01-SUMMARY.md`
</output>
