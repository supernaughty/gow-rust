---
phase: 09-external-bundling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extras/bin/egrep.bat
  - extras/bin/fgrep.bat
  - extras/bin/bunzip2.bat
  - extras/bin/gawk.bat
  - extras/bin/gfind.bat
  - extras/bin/gsort.bat
  - extras/bin/gzip.bat
  - extras/bin/unxz.bat
autonomous: true
requirements:
  - BND-03

must_haves:
  truths:
    - "Legacy GOW names egrep, fgrep, bunzip2, gawk, gfind, gsort, gzip, unxz are committed to git source control"
    - "Each .bat shim invokes the correct Rust binary via %~dp0-relative path"
    - "A fresh git clone (no download-extras.ps1 run) contains the shim files and they are valid batch scripts"
  artifacts:
    - path: "extras/bin/egrep.bat"
      provides: "egrep shim -> grep.exe -E"
      contains: "grep.exe -E"
    - path: "extras/bin/fgrep.bat"
      provides: "fgrep shim -> grep.exe -F"
      contains: "grep.exe -F"
    - path: "extras/bin/bunzip2.bat"
      provides: "bunzip2 shim -> bzip2.exe -d"
      contains: "bzip2.exe -d"
    - path: "extras/bin/gawk.bat"
      provides: "gawk shim -> awk.exe"
      contains: "awk.exe"
    - path: "extras/bin/gfind.bat"
      provides: "gfind shim -> find.exe"
      contains: "find.exe"
    - path: "extras/bin/gsort.bat"
      provides: "gsort shim -> sort.exe"
      contains: "sort.exe"
    - path: "extras/bin/gzip.bat"
      provides: "gzip shim -> gzip.exe"
      contains: "gzip.exe"
    - path: "extras/bin/unxz.bat"
      provides: "unxz shim -> xz.exe -d"
      contains: "xz.exe -d"
  key_links:
    - from: "extras/bin/*.bat"
      to: "git index"
      via: "git add + commit"
      pattern: "git ls-files extras/bin/*.bat"
---

<objective>
Commit the eight batch alias shims to git source control so they ship with the repository and are available to contributors and CI without running download-extras.ps1.

Purpose: The .bat files are already generated by download-extras.ps1 and present locally, but untracked. Committing them ensures fresh clones get the shim files immediately, legacy GOW compatibility names work without running the download script, and the WiX installer stage step can copy them from source.

Output: extras/bin/egrep.bat, fgrep.bat, bunzip2.bat, gawk.bat, gfind.bat, gsort.bat, gzip.bat, unxz.bat committed to the develop branch.
</objective>

<execution_context>
@/c/Users/super/workspace/gow-rust/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/super/workspace/gow-rust/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/super/workspace/gow-rust/.planning/PROJECT.md
@/c/Users/super/workspace/gow-rust/.planning/ROADMAP.md
@/c/Users/super/workspace/gow-rust/.planning/STATE.md
@/c/Users/super/workspace/gow-rust/.gitignore
@/c/Users/super/workspace/gow-rust/download-extras.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify shim content and commit batch alias files to git</name>
  <files>
    extras/bin/egrep.bat
    extras/bin/fgrep.bat
    extras/bin/bunzip2.bat
    extras/bin/gawk.bat
    extras/bin/gfind.bat
    extras/bin/gsort.bat
    extras/bin/gzip.bat
    extras/bin/unxz.bat
  </files>

  <read_first>
    - extras/bin/egrep.bat — confirm content is `@echo off & "%~dp0grep.exe" -E %*`
    - extras/bin/fgrep.bat — confirm content is `@echo off & "%~dp0grep.exe" -F %*`
    - extras/bin/bunzip2.bat — confirm content is `@echo off & "%~dp0bzip2.exe" -d %*`
    - extras/bin/gawk.bat — confirm content is `@echo off & "%~dp0awk.exe" %*`
    - extras/bin/gfind.bat — confirm content is `@echo off & "%~dp0find.exe" %*`
    - extras/bin/gsort.bat — confirm content is `@echo off & "%~dp0sort.exe" %*`
    - extras/bin/gzip.bat — confirm content is `@echo off & "%~dp0gzip.exe" %*`
    - extras/bin/unxz.bat — confirm content is `@echo off & "%~dp0xz.exe" -d %*`
    - .gitignore — confirm `extras/bin/*.bat` is NOT listed as ignored
  </read_first>

  <action>
1. Read each of the 8 .bat files listed above to confirm their content matches the pattern from download-extras.ps1:
   - egrep.bat  → `@echo off & "%~dp0grep.exe" -E %*`
   - fgrep.bat  → `@echo off & "%~dp0grep.exe" -F %*`
   - bunzip2.bat → `@echo off & "%~dp0bzip2.exe" -d %*`
   - gawk.bat   → `@echo off & "%~dp0awk.exe" %*`
   - gfind.bat  → `@echo off & "%~dp0find.exe" %*`
   - gsort.bat  → `@echo off & "%~dp0sort.exe" %*`
   - gzip.bat   → `@echo off & "%~dp0gzip.exe" %*`
   - unxz.bat   → `@echo off & "%~dp0xz.exe" -d %*`

2. Confirm .gitignore does NOT contain a line matching `extras/bin/*.bat` (the current .gitignore only ignores `extras/bin/*.exe` and `extras/bin/vim-runtime/` — the .bat files are intentionally NOT ignored).

3. If any file is missing or has wrong content, write the correct content using the exact strings above (ASCII encoding, `@echo off &` one-liner format).

4. Stage and commit all 8 files:
   ```
   git add extras/bin/egrep.bat extras/bin/fgrep.bat extras/bin/bunzip2.bat \
           extras/bin/gawk.bat extras/bin/gfind.bat extras/bin/gsort.bat \
           extras/bin/gzip.bat extras/bin/unxz.bat
   git commit -m "feat(09): commit batch alias shims for legacy GOW compatibility (BND-03)"
   ```

Note: vim.bat is already tracked (git ls-files confirms it). Do NOT re-add or re-commit vim.bat.
  </action>

  <verify>
    <automated>git -C /c/Users/super/workspace/gow-rust ls-files extras/bin/egrep.bat extras/bin/fgrep.bat extras/bin/bunzip2.bat extras/bin/gawk.bat extras/bin/gfind.bat extras/bin/gsort.bat extras/bin/gzip.bat extras/bin/unxz.bat | wc -l</automated>
    Expected output: `8` (all 8 files tracked)
  </verify>

  <acceptance_criteria>
    - `git ls-files extras/bin/*.bat` lists exactly 9 files (the 8 aliases + vim.bat already tracked)
    - `git show HEAD --name-only` includes all 8 alias .bat filenames in the commit
    - `git status extras/bin/` shows no untracked .bat files
    - Each .bat file content matches: `grep -c "grep.exe -E" extras/bin/egrep.bat` returns `1`
    - Each .bat file content matches: `grep -c "bzip2.exe -d" extras/bin/bunzip2.bat` returns `1`
    - `git log --oneline -1` shows a commit message containing "BND-03"
  </acceptance_criteria>

  <done>All 8 batch alias .bat files are committed to git. `git ls-files extras/bin/` shows 9 .bat files (8 aliases + vim.bat). No untracked .bat files remain. Fresh clone will include all shims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .bat shim -> Rust binary | Batch file invokes exe by %~dp0-relative path; no user input involved in the shim itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | extras/bin/*.bat | accept | Files are committed to git with SHA integrity; any modification would show as a diff; low-value target (no PII, just shims) |
| T-09-01-02 | Spoofing | %~dp0 path resolution | accept | %~dp0 resolves to the directory of the .bat file itself; attacker would need write access to the install directory to plant a rogue exe, at which point the system is already compromised |
</threat_model>

<verification>
After committing:
1. Run: `git ls-files extras/bin/` — expect 9 .bat files
2. Run: `git status` — expect clean working tree for extras/bin/
3. Run: `type extras\bin\egrep.bat` (Windows) — expect `@echo off & "%~dp0grep.exe" -E %*`
4. Run: `git log --oneline -5` — expect the commit at HEAD with "BND-03" in message
</verification>

<success_criteria>
- All 8 batch alias files are tracked in git (git ls-files returns all 8)
- No untracked .bat files in extras/bin/
- Each file delegates to the correct Rust binary via %~dp0-relative path
- A clean `git clone` of the repo will include all 8 shim files without running download-extras.ps1
</success_criteria>

<output>
After completion, create `.planning/phases/09-external-bundling/09-01-SUMMARY.md` using the summary template.
</output>
